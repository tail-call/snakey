{
  "scripts": {
    "serve": "esbuild --outdir=www/js --servedir=www src/index.ts",
    "test": "esbuild --bundle ./tests/index.ts --outdir=test-bundle --sourcemap && node --enable-source-maps test-bundle"
  },
  "devDependencies": {
    "@types/chai": "^4.3.1",
    "chai": "^4.3.6",
    "esbuild": "^0.14.49"
  }
}
